def gv
pipeline{
    agent any
    stages{
        stage("Initialize groovy script"){
           steps{
               script{
                   sh 'chmod +r script.groovy'

                   gv = load "script.groovy"
               }
           } 
           
        }
        stage("Build Image"){
            steps{
                script{
                    gv.buildImage()
                }
            }

        }
        
    }
    
}
